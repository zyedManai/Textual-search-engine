<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title elastic
        style
            include ../public/styles.css
    body
        header
        main(class="px-16 py-16 bg-grey-800")
            form(action="/", method="POST" class="text-center my-10 h-20 text-5xl")
                input(type="text", name="tag", placeholder="Search" class="shadow-md border-dotted border-4 mx-2 border-gray-600 rounded")
                input(type="submit", class="bg-blue-500 hover:bg-blue-400 text-white font-bold my-2 px-4 border-b-4 border-blue-700 hover:border-blue-500 rounded")
            br
            div(class="grid grid-cols-4")
                each photo in photos
                    div(class="bg-white rounded overflow-hidden shadow-md hover:shadow-2xl mx-3 my-3 h-30")
                        img(src=photo, class="w-full object-cover scale-75")